/var/log/nginx/*.log {
  daily
  dateext
  dateformat -%Y%m%d
  notifempty
  missingok
  rotate 30
  compress
  delaycompress
  copytruncate
  sharedscripts
  postrotate
          [ -s /run/nginx.pid ] && kill -USR1 `cat /run/nginx.pid`
  endscript
}
